{
  "name": "aculich-github-io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/generate-site-config.js",
    "build": "eleventy",
    "clean": "node -e \"require('fs').rmSync('blog',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "markdown-it-anchor": "^9.2.0"
  }
}
